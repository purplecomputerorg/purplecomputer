[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["F"]  # pyflakes only: undefined names, unused imports, etc.

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["F811"]  # allow fixture redefinition
