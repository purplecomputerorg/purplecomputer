# Copy to .env and fill in. The .env file is gitignored.
# Used by the AI UX testing scripts (scripts/ai_ux_*.py, `just ux`), which
# extract only ANTHROPIC_API_KEY from it, so it can also be a symlink to a
# central machine secrets file.

ANTHROPIC_API_KEY=
