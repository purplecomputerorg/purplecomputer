#!/usr/bin/env bash
set -euo pipefail

LANE="${1:-}"

if [ -z "$LANE" ]; then
  echo "usage: ./scripts/cw LANE_NAME [extra claude args...]"
  exit 1
fi

shift || true

exec claude --worktree "$LANE" "$@"
